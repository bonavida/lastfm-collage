import React from 'react';
/** Styles */
import './Header.scss';

const Header = () => (
  <div className="header__container">
    <h1 className="header__title">lastfm Collage</h1>
  </div>
);

export default Header;
